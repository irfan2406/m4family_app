import 'package:flutter/widgets.dart';

class LucideIconData {
  // LucideIconData no longer extends IconData because IconData is marked final in newer Flutter versions.
  // We use direct IconData instantiations in lucide_icons.dart.
}

